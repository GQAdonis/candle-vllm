# Local kubeconfig files (never commit)
*.kubeconfig
client-cluster.kubeconfig

# gcloud auth plugin cache
gke_gcloud_auth_plugin_cache

# Terraform/OpenTofu local artifacts
.terraform/
.tofu/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Rust build artifacts
target/
